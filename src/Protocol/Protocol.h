#ifndef LAMP_PROTOCOL_H
#define LAMP_PROTOCOL_H

#include "Standard/Standard.h"
#include "Color/Color.h"
#include "Off/Off.h"
#include "Random/Random.h"
#include "Reset/Reset.h"

namespace Lamp {
    namespace Protocol {
        class Protocol {
        private:

        protected:

        public:
            Protocol();
            ~Protocol();
        };
    }
}
#endif //LAMP_PROTOCOL_H
